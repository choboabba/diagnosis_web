# Diagnosis Project - Claude Working Rules

## 1. 프로젝트 목적

이 프로젝트는 부모성장연구소 / 행복대물림OS의 부모유형진단 페이지를 개발하기 위한 프로젝트다.

핵심 목적은 다음과 같다.

1. 사용자가 무료 부모유형진단을 진행한다.
2. 진단 결과를 기반으로 무료 진단 리포트를 제공한다.
3. 유료 상세 진단 리포트로 자연스럽게 연결한다.
4. 이후 매거진, 챌린지, 미니코스, VOD 상품으로 연결 가능한 기반을 만든다.

## 2. 절대 금지 사항

Claude는 다음 작업을 사용자 승인 없이 수행하면 안 된다.

- 실제 고객 이메일 자동 발송
- 실제 고객 데이터 삭제
- Google Sheets 전체 범위 수정
- `.env` 파일 내용 출력
- API Key, Webhook URL, 토큰 출력
- 결제/환불 자동 처리
- 의료, 발달, 심리 내용을 출처 없이 단정
- 요청받지 않은 인접 코드 리팩토링
- UI 스타일을 임의로 전면 변경
- 기존 정상 작동 기능을 확인 없이 변경

## 3. 작업 원칙

모든 작업은 다음 순서를 따른다.

1. 먼저 현재 요청의 목적을 요약한다.
2. 수정할 파일 목록을 먼저 제시한다.
3. 수정 전 현재 구조를 확인한다.
4. 필요한 최소 범위만 수정한다.
5. 수정 후 테스트 방법을 제시한다.
6. 완료 선언 전 검증 결과를 제시한다.

## 4. 코딩 원칙

- 실제 코드에 존재하는 함수명과 변수명만 근거로 설명한다.
- 파일을 읽지 않은 상태에서 존재하지 않는 함수명, 변수명, 파일명을 만들지 않는다.
- 한 번에 여러 기능을 섞어 수정하지 않는다.
- 기능 수정과 디자인 수정은 분리한다.
- 테스트 데이터와 실제 고객 데이터를 분리한다.
- 개인정보는 diagnosis_id 기준으로 다룬다.

## 5. 진단 비즈니스 기준

무료 진단 결과는 빠른 자기이해를 제공한다.

유료 상세 진단 결과는 다음 정보를 더 깊게 제공한다.

- 문제 원인 해석
- 3No 유형 분석
- 영역별 점수 해석
- 우선 해결 과제
- 추천 매거진
- 추천 가이드
- 추천 챌린지
- 다음 행동 제안

## 6. 보안 기준

다음 작업은 반드시 사용자 승인 후 진행한다.

- 이메일 발송 로직 수정
- Google Sheets 쓰기 로직 수정
- 고객 데이터 구조 변경
- 결제 관련 코드 변경
- 배포 설정 변경
- 환경변수 변경
- Git commit, push, PR 생성

## 7. 탐색 제외 경로

`del_file/` 폴더는 삭제 예정 파일 보관 폴더다.
이 폴더 내 파일은 읽거나 탐색하지 않는다.
코드 탐색, grep, glob 작업 시 `del_file/`를 항상 제외한다.

## 8. Claude Forge 사용 기준

새 기능 개발은 다음 흐름을 따른다.

- `/plan`
- `/tdd`
- `/code-review`
- `/security-review`
- `/handoff-verify`
- `/sync`

버그 수정은 다음 흐름을 따른다.

- `/explore`
- `/tdd`
- `/verify-loop`
- `/quick-commit`
- `/sync`

보안 검토는 다음 흐름을 따른다.

- `/security-review`
- `/stride-analysis-patterns`
- `/security-compliance`